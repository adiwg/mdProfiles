{
  "increment": "patch",
  "git": {
    "tagName": "v${version}",
    "requireCommits": true
  },
  "hooks": {
    "before:init": [
      "rm -rf json/*",
      "npm test",
      "git checkout -- dist/* index.js"
    ],
    "after:bump": "npm run build",
    "after:release": [
      "mkdir json; cp resources/* json -r",
      "npx replace-in-file VERSION \"${version}\" json/**/*.json",
      "git push upstream master:upstream/gh-pages"
    ]
  }
}
